<p id="notice"><%= notice %></p>

<h1>Listing Professors</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Uid</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @professors.each do |professor| %>
      <tr>
        <td><%= professor.name %></td>
        <td><%= professor.uid %></td>
        <td><%= professor.description %></td>
        <!-- <td><%= link_to 'Show', professor %></td> -->
        <td><%= link_to 'shedule Appointment', edit_professor_path(professor) %></td>
        <!-- <td><%= link_to 'Destroy', professor, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

